import styled from "styled-components";

export const Container = styled.div({
  maxWidth: 1440,
  width: "100%",
  margin: "auto",
});

export const Title = styled.h1({
  fontSize: 30,
  fontWeight: 500,
});
